# Find PI to the Nth Digit

This Python program calculates the value of π (pi)
up to N decimal places using high-precision arithmetic.

## Features
- User-defined precision
- Input validation
- Safety limit
- Uses Python's `decimal` module

## How to run
```bash
python pi_to_n_digits.py
